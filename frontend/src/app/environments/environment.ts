export const environment = {
  production: false,
  baseUrl: 'http://localhost:8080',
  apiEndpoints: {
    connection: '/api/connection',
    connect: '/api/connect',
    topics: '/api/topics',
    consumers: '/api/consumers',
    cluster: '/api/cluster'
  }
};
